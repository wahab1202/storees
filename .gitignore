node_modules/
dist/
.next/
.env
.env.local
*.tsbuildinfo
.DS_Store
.turbo/

# Local planning / Claude config (not for repo)
Storees Phase 2 Claude.md
Storees Phase 2 Folder/

# Redis writes its snapshot into whatever directory it was started from, so a
# redis-server launched here drops a 300KB memory dump in the repo root. It is
# Redis's own data, nothing in Storees reads it, and it has never been tracked.
dump.rdb
*.rdb

# Dev-server logs from local `npm run dev` / uvicorn runs. Local runtime noise,
# nothing in Storees reads them, and they have never been tracked.
.devlogs/
